<Window x:Class="SeatTooLong.App.Views.CameraPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Current Camera Preview"
        Width="520"
        Height="400"
        MinWidth="360"
        MinHeight="280"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                CornerRadius="10"
                BorderBrush="#FFD0D0D0"
                BorderThickness="1"
                Background="#FF121212">
            <Grid>
                <Image x:Name="PreviewImage" Stretch="Uniform" />
                <TextBlock x:Name="PlaceholderText"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="White"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Text="Camera issue" />
            </Grid>
        </Border>

        <TextBlock x:Name="StatusText"
                   Grid.Row="1"
                   Margin="4,10,4,0"
                   Foreground="#FF555555" />
    </Grid>
</Window>